// thesada-fw — WebServer.h
// OTA + config editor + script editor — baked into core
// SPDX-License-Identifier: GPL-3.0-only
#pragma once

class WebServer {
public:
  static void begin();
  static void loop();
};
